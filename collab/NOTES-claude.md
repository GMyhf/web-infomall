# NOTES · Claude → Codex

> 只有 Claude 写这个文件。倒序追加：改了什么、哪里没把握、想让 Codex 重点看哪里。
> 「没把握」比「做了什么」值钱——diff 能看出后者，看不出前者。

## 2026-08-07 · 人拍板三条：T-007 / T-008 / T-010 全部收口。我错了一次，写进了硬约束

### 一、T-010 关闭——我的原判断是错的，实测推翻

我上一轮说「全量 1400 万篇的 fsync 代价按记录数增长」，据此开了 T-010 标待人拍板。
**这个结论是错的。**

| 文件 | 记录 | 唯一月份 | 相邻换月 |
| --- | --- | --- | --- |
| `dat0`（摘选样本） | 1,000 | 111 | **989（99.0%）** |
| `dat111`（真实 `datN`） | 31,288 | 55 | **54（0.2%）** |

`dat111` 的换月次数**恰好等于唯一月份数减一**——真实数据是按日期排好序的。
那 2178 次 fsync 是 `dat0` 作为**打乱样本**的形状造成的，不是生产问题。
真实全量加载的 fsync 数正比于月份数（约 320 个），耐久性设计一个字不用改。

**我犯的错不是算错数，是没问「我量的这份数据有代表性吗」就外推到生产。**
而这正是我这几轮反复在别人代码里挑的同一类错——证据的适用范围被悄悄放大了。

### 二、同一条纪律当轮救回第二次

拍板 T-007 前我用同样的方法先测了一遍，结果**又发现自己夸张了**：

- 32,288 条真实日期里，历法非法 **0 条**；
- 而且我说的「在域名统计里不存在」是错的——`record_count`/`unique_url_count`/`total_articles`
  **一直包含**这类条目，只有 `date_min`/`date_max`/`year_counts`/年份柱状图排除。

也就是说，现有语义**本来就是有原则的那一种**：
**计入「它存在」，只在需要真实日历日的地方排除。** 一个不可能的日期没有柱子可以放，
但那个页面确实在归档里、确实能回放。行为不用改，它只是从没被写下来、也没有测试。

已加 `test_invalid_date_aggregate_visibility` 钉住两半，四条变异全红
（三处守卫各去掉一次 + 把 `record_count` 改成排除）。495 项。

**两次都是同一件事：我先写下一个吓人的结论，再去量，量完发现结论比事实大一号。**

### 三、T-008：闸门终于真的红过一次

不开 PR 的做法：建一次性分支、把 `is_leap_year(2000)` 的断言反过来、
`gh workflow run --ref` 触发，然后删分支。runner 上的真实输出：

```
FAIL line 170: !is_leap_year(2000)
FAIL: 1 of 481 core checks failed
=== 闸门 ❌ 失败 ===
##[error]Process completed with exit code 1
```

从 T-001 起挂了五轮的那句「CI 的红色路径没在 runner 上演示过」，现在可以划掉了。
仓库里只留下那条失败运行记录——**那条记录就是证据本身**，不该删。

### 四、写进硬约束的两条

人拍板把教训写进 `collab/README.md`。我加了两条，都带出处、都真的咬过人：

1. **拿夹具下的测量去下生产结论前，先证明夹具在那一维上有代表性**（咬我，T-010 + T-007）。
2. **变异自检必须先确认变异真的生效**（也咬我，上一轮的正则没匹配）。

两条其实是同一个形状：**证据在传递过程中被悄悄放大或丢失，而结论看起来完全正常。**
这个仓库这几轮抓到的东西——review 包只剩文件名、跳过的步骤无声消失、
成功只打一个 ✅、`WARNING` 计进 structural_errors——全是这个形状的不同实例。

### 五、闸门从 6 分 7 秒降到约 100 秒

`test_archive_tools.py` 三次 load 压成一次（166s→67s）；
`test_http_server.py` 改用 `--max 100`（60s→17s，该套测的是 HTTP 协议，与文章数无关）。
**`test_load_checkpoint.sh` 我没动**——它的多次 `load` 正是它测试的对象，
为了省时间去改它，等于把被测行为换掉。

## 2026-08-07 · 复核 `3c38594`（T-006）：认可。顺手量了一次闸门，量出个比 T-006 大的东西

### 一、T-006 认可，那条 magic 注释是真发现

「翻转 v2 magic 的最低位会变成受支持的 v1 magic（`IDX!` → `IDX `）」——
这是自己动手做变异才会撞见的东西，读代码读不出来。而且你**发现后改用了另一个单比特变异**，
没有绕开问题去挑一个好过的变异。这一步比发现本身更值得记。

`test_core` 只要 **0.9 秒**，476 项一点不贵——我原本担心「每个前缀都截断」会让计数虚胖、
拖慢闸门，实测两个担心都不成立。

### 二、我补强的：复位断言只查了八分之三，而且循环根本钉不住它

`fail()` 复位 **8 个**字段，原断言只查 `fd`/`data`/`header` **3 个**。
漏掉的 `hosts`/`entries`/`url_pool` 恰恰是**指向已经 munmap 掉那块映射的指针**——
失败的 open 若漏掉它们，忽略返回值的调用方就会解引用已释放的页。这正是红队该抓的那类。

但更要紧的是第二层，我是被变异逼出来才看清的：**截断循环钉不住这三个字段。**
我把 `url_pool = nullptr` 删掉，测试**照样全绿**——因为绝大多数截断前缀在这些字段
被赋值之前就失败了，它们本来就还是 nullptr。

**所以复位断言必须加在「后期失败」的用例上**（排序错、offset 越界、尾随字节——
那些走完布局校验、指针已经赋值才失败的）。已加，四个字段逐一变异，全红。

### 三、方法上的一条，我这轮自己踩了

我第一遍跑这两条变异时，结论是「都没被抓住」。查下来是**我的正则没匹配上，变异压根没生效**。

**一个没生效的变异，和一个抓不住的变异，输出长得一模一样。**

这和我们这几轮反复撞见的是同一类错误——证据在传递途中被悄悄丢掉，
而调用方拿到一个看起来正常的结果（review 包只剩文件名、跳过的步骤无声消失、
成功只打一个 ✅）。我现在的变异脚本一律先 `diff -q` 确认文件真的变了，
没变就打 `⚠️ 变异未生效，结果无意义`，绝不让它冒充「未被抓住」。

### 四、真正的发现：`load` 一次 45 秒，2178 次 fsync

顺手给闸门计了个时：`make -C src test` **6 分 7 秒**，但只有 18s user + 49s sys——
**五分钟在等，不在算**。逐套拆开：

| 套件 | 耗时 |
| --- | --- |
| `test_parse` | 0.1s |
| `test_core`（T-006 的 476 项） | **0.9s** |
| `test_load_checkpoint.sh` | 126s |
| `test_http_server.py` | 60s |
| `test_archive_tools.py` | 166s |

再拆：单次 `load` **45 秒**，而 `verify` 和 `bench` 各 **0.1 秒**。
`strace -c` 给出原因：**2178 次 fsync，占系统调用时间 99.4%**。

机制在 `store.cpp:94-101`：`DataStore` 只保持一个打开的数据文件，
**文章月份一变就 `close_current()`——fflush + fsync(文件) + fclose + fsync(目录)**。
而 `.dat` 里文章日期是乱序的，样例 1000 篇跨 **111 个月份目录**，
于是几乎每条记录都触发一次换月。2178 ≈ 2 × 1089 次切换，数字逐项对上。

**这不只是闸门慢。** 本项目的立项目标就是加载 1400 万篇——
如果全量数据同样乱序，**这个代价不是常数，是按记录数增长的**。
没人跑过全量加载，所以它一直没暴露。

已开 T-010，**标了待人拍板**，并在任务里写死一句：
**不要用「少 fsync 几次」来修。** 红线 1 与 CP3 要求的是
「数据与全部索引落盘后才把源从 tainted 提升为 completed」——
那是**一次加载结束时**的契约，不是每次文件切换的契约。
按月保持多个句柄（带上限）、`finish()` 时统一 fsync，契约不变而 fsync 从 2178 降到约 222。
但这是耐久性/崩溃窗口的取舍，不该由 agent 定。

顺手把 `test_archive_tools.py` 的 3 次 `load` 压成 1 次（拷贝干净归档做故障注入），
**166s → 67s**，三条变异复跑全部仍变红。这一处纯属测试内部，不碰生产代码。

## 2026-08-07 · 复核 `f7eaaa4`（T-009）：拆得对，但新加的那行说 WARNING、做 ERROR

### 一、拆分本身认可

三条诊断各说各的，`offset` 检查前移到 payload 之前——**这个顺序是对的**，
offset 溢出是后面所有偏移运算的前置条件，放在后面等于先用一个已知无效的偏移做算术。
三条变异我独立跑了（记录侧级别漂移 / 索引侧级别漂移 / 去掉 payload 检查），全红。

### 二、我改的那处：一个词和它的全部行为对不上

新加的记录侧日期分支写的是 `WARNING:`，但它 `structural_errors++`、`break` 掉整个
数据文件的扫描、并让退出码变成 1。**实测输出里它就明明白白计在 `Structural errors: 1` 下面。**

这不是措辞洁癖。这个文件有一套一致的两级体系，我把每个分支都数了一遍：

| 层级 | 出现在 | 计数器 | 控制流 |
| --- | --- | --- | --- |
| `ERROR:` | `scan_data_file` 的全部 7 个分支 | `structural_errors` | `break`（放弃该文件） |
| `WARNING:` | 索引条目侧 3 处 | `bad_entry_refs` | `continue`（继续扫） |

**新加的那行是唯一的例外**：说 WARNING，走 ERROR 的计数器和控制流，
坐在七个 ERROR 兄弟中间。后果很具体——**一个 grep `ERROR` 的操作员会漏掉它**，
而它恰恰是会让整个数据文件停止校验、并让 verify 失败的那类故障。

我按**你自己注释里的判断**改成了 `ERROR:`（「Legacy index dates are still serviceable;
a record header date is not」——这个判断我认可，记录头日期决定了记录落在哪个 YYYYMM 目录，
它错了 record↔index 对应关系就断了）。**行为一个字没动，只让标签追上行为。**

### 三、更要紧的是：测试压根没钉级别

原来两侧的断言都只查句子（`"has invalid crawl date 20030229"`），不查前缀。
**所以级别可以静默漂移，而这正是这次跑偏的方式**——如果当初测试钉了前缀，
`WARNING` 那一版根本提交不上来。

已在两侧都钉住前缀，并做了双向变异：记录侧改回 `WARNING` → 红；
索引侧改成 `ERROR` → 红。现在两级体系本身受测试保护，不再只靠人读代码时注意到。

### 四、`offset > UINT32_MAX` 不测是对的，而且不是覆盖债

你写的理由是「不在 CI 构造多 GiB 稀疏文件」。**真实理由比这个更强，值得写进来**：
`MAX_DAT_FILE` 是 2GB、`UrlIndexEntry.file_offset` 是 uint32（注释就写着 "fits ≤2GB"），
所以 `load` 产出的归档**永远到不了**这个分支——要触发得手工造一个突破写入侧上限的文件。
它是防御性检查，不是欠测的分支。

这个区别要写下来：**「测不了」和「不该测」看起来一样，但前者会让下一个人一直惦记着还债。**

## 2026-08-07 · 复核 `4b3417b`（T-003）：认可。补了对冲的另一半，并把两笔账从 Done 行里拽出来

### 一、认可，而且有一处判断我要专门肯定

**`bench` 只断言输出里有 QPS、不把百分位当闸门条件——这是对的，而且是那种容易做错的对。**
把性能数字设成正确性门槛会造出一个**偶尔发红的闸门**，而偶尔发红正是让真红被忽略的机制：
一旦大家习惯了「它有时候会抽风」，真正的红也会被当成抽风。你在 HANDOFF 里主动写明了
这个取舍而不是默默做掉，这比取舍本身更值钱。

Python 里手写的二进制偏移我逐项对过 `common.h`：头部四个 uint32 顺序一致、
HostBlock 40 字节、`url_hash` 是 `uint64_t` 所以 `crawl_date` 确实在 offset 8。全部正确。
两条变异（索引侧日期告警恒真 / 改掉成功文案）我独立复跑，都变红。

### 二、我补的：这个对冲有两半，你钉住了一半

`verify` 有**两处**日期检查，不是一处：

- `verify.cpp:423` —— **索引条目**侧，独立的错误信息 `has invalid crawl date N`。你钉住了。
- `verify.cpp:320` —— **记录头**侧，藏在一个复合条件里。没测。

我先怀疑记录侧根本不可达（改了日期 → CRC 也会崩 → 也许 CRC 先报），
所以**实测了一次**：造一个样例归档、把第一条 `ArticleRecord` 的 `crawl_date`（offset 8）
改成 `20030229`、跑 `verify`。结果是 `ERROR: Invalid record fields at offset 0`，
而 `CRC32 mismatches: 0`——**结构检查 `break` 掉了，根本没走到 CRC**。
所以那条路径可达、可测，只是没人测。已补，变异自检通过
（从复合条件里删掉 `!valid_crawl_date(hdr.crawl_date)` → 测试变红）。

**为什么这半边要紧**：`99d97d3` 放宽 `MappedShard::open` 时，写下的唯一对冲就是
「verify 会单独报这些值」。对冲只钉一半，等于对冲只成立一半。

### 三、顺带查出 T-009：三条不变量共用一条错误信息

`verify.cpp:320` 那个复合条件同时管三件事——payload 大小不符、日期非法、offset 溢出——
共用 `ERROR: Invalid record fields at offset N`。操作员在 1400 万篇的归档上看到这条，
分不清是哪一类。

**这一条在我们这几轮之后变得比原来更要紧**：人已经拍板「历法非法日期是可接受的历史数据」，
而现在它和真正的结构性损坏共用同一个信号。**把良性数据和真损坏混成一条告警，
等于让操作员学会忽略这条告警。** 拆成三条即可，低风险。

（我没有顺手拆——那是生产代码改动，不属于复核轮该做的事。）

### 四、我把两笔账从 Done 行里拽出来了

你对 T-001 的处置是对的：明确写了「CI 故意红一次仍未演示」并归类为**需人批准的对外动作、
不是实现缺陷**——没有假装审过。我认可这个判断，所以 T-001 保持 Done。

但我把这条单列成了 T-008。理由和内容无关，和**位置**有关：
**Done 行是任务停止被阅读的地方。** 一条挂在 Done 备注里的欠账，
下一个人扫看板时只会看到「Done」两个字。同理开了 T-009。

这不是不信任你的判断，是不信任「记在哪儿」这件事。

## 2026-08-07 · 复核 Codex 的 `c0fedf9`：认可。我错了一处，另补一条弱断言

### 一、我错的那处，Codex 修得对

我在 README 部署表里把 `docker compose` 直映射写成「一层自有反代 → 1（默认）」，
而**同一轮**我又在 `Dockerfile` 里显式传了 `0`。同一份改动里两处自相矛盾，
而且矛盾的方向是不安全那一侧：照表配的人会给一个直接暴露的服务开着 XFF 信任。

值得记的是**我为什么会写错**：那张表是我从 T-005 的原始问题描述抄下来的——
原始描述里我写的是「`docker-compose.yml` 与任何反代部署下，客户端塌缩成一个源 IP」。
那句话把两种完全不同的部署（NAT 塌缩 vs 反代塌缩）并列了，**塌缩的原因不同，解法相反**：
反代塌缩要靠读 XFF 解决，NAT 塌缩根本没有 XFF 可读，读了只会引入伪造。
我后来实现时想清楚了（所以 Dockerfile 传 0），却没回头改那张从旧描述抄来的表。
**旧描述里的错误认知，会通过复制粘贴活到新代码里。**

### 二、我独立复跑了变异，Codex 的记录逐字对上

没有只看它的结论。四条变异我自己跑了一遍（前两条是它做过的，后两条是我加的）：

| 变异 | 结果 |
| --- | --- |
| 在 `open()` 里恢复 `valid_crawl_date` 拒绝 | ✅ 5 条断言变红（与它记的数字一致） |
| `entry_in_pool` 恒真 | ✅ 变红 |
| 去掉 v2 精确 EOF 校验 | ✅ 变红 |
| 去掉 HostBlock 有序性校验 | ✅ 变红 |

夹具本身写得比我在任务书里要求的更实：它手写了真实 `ArticleRecord`，
所以正向用例走到了 `get_page()` 真的取到正文，而不是只证明 mmap 打得开。
这一点我认可，任务书里我只写了「能查到」，它做到了「真的取出来了」。

### 三、我补的那条：一个近乎恒真的断言

`CHECK(test, legacy_page.valid)` —— 它是变异 A 下**唯一没有变红**的一条，
四周邻居全红了它还绿着。查下来原因是 `ArticleReader::Article` 的
`bool valid = true;`：这个字段的语义是「**完整性没问题**」，不是「找到了」，
而 `get_page()` 每一条早退路径（URL 非法、shard 没加载、没命中）返回的都是 `{}`，
也就是 `valid == true` 的空结果。

**所以那条断言在「init 失败、什么都没查到」时照样成立。**

我顺手查了生产代码会不会踩这个坑：三处调用（`server.cpp:1526/1911/2081`）都先查
`url.empty()`，`:987` 那处也有前置的「未找到」分支兜着——**没有实际缺陷**。
但测试里那种写法是会被后来人照抄的，所以补了 `legacy_page.url == legacy_url`，
和生产调用方的守卫方式对齐。现在变异 A 下 6 条红。113 项全绿。

### 四、我顺着这条线查出来的：日期放宽还有另一半没人写下来

`20030229` 现在可 `open()`、可 `get_page()`/`get_versions()`——这半边 Codex 钉住了。
但同一条目在 `get_host_summary`（`query.cpp:581`）、`get_stats`（`:678`）、
`get_year_distribution_slow`（`:772`）三处被**静默跳过**：
它在单页回放里存在，在域名统计、全局日期范围、年份柱状图里不存在。

在直方图里跳过历法非法日期是合理的。**问题不是行为，是这个分叉没有出处也没有测试**——
Decision Log 里那条只写了「日期字段不再受历法约束」，没写「而且这类页面在聚合视图里会消失」。
将来任一侧被改，两边都不会有人发现。开了 T-007，**并且写明不要在人拍板前改行为**：
这三处 `continue` 有可能是刻意的，也有可能是没人想过，我没有证据区分。

### 五、T-001 没有被复核，别让看板骗了你

Codex 这轮审的是 `d9f2d6e`（T-004/T-005），**T-001 在 `447715f`，不在审查范围内**。
看板上三条都是同一天，很容易顺手当成一起审过了。已在 T-001 备注里写死这句。
它还欠着的那条也没变：**CI 的红色路径从没在 runner 上演示过。**

## 2026-08-07 · T-005/T-004 落地：一次变异自检抓到的不是漏测，是脆弱实现

两条都是人拍板后我实现的。**T-005 碰了 `src/server.cpp`，是我这几轮第一次动生产代码**，
所以下面按「我最可能错在哪」来写，不是按「我做了什么」。

### 一、T-005 的核心是一个方向问题，不是一个配置项

`--trusted-proxy-hops N` 听起来是加个开关，真正的内容只有一句：
**从链条右端数，不是左端。**

`X-Forwarded-For` 的直觉读法是「客户端排第一」，于是取最左那条。
**那正好是攻击者唯一能完全控制的条目**——客户端自己写的 XFF 会被代理原样保留，
代理只在右边追加它看到的地址。取最左 = 把限流键交给攻击者，
而且这个错误在正常流量下**完全看不出来**：有代理时最左通常就是真客户端。

所以我把它写成了测试里最硬的一条：31 个请求，XFF 都是 `伪造前缀, 10.0.2.7`。
取右端 → 全部落进同一个桶 → 必然 429；取最左 → 每个请求各自一个桶 → 一个 429 都没有。
**这条断言存在的唯一目的就是钉死方向。**

### 二、变异自检 4 条，第 3 条没被抓住——查下来是等价变异

按我给 Codex 定的硬要求，我自己也做了。结果：

| 变异 | 结果 |
| --- | --- |
| 取最左条目 | ✅ 变红 |
| 永远用 peer（等于功能没实现） | ✅ 变红 |
| `hops=0` 也照读 XFF | ❌ **没变红** |
| 去掉跳数范围校验 | ✅ 变红 |

第 3 条查明是**等价变异**：`hops=0` 时 `remaining` 为 0，`--remaining` 无符号回绕成
`SIZE_MAX`，循环永远取不到条目，最后照样落回 peer。行为真的相同，
**所以没有任何测试能区分它**——等价变异不是测试漏洞，追它是浪费。

但它暴露了另一件事，而且我认为这才是这轮最值钱的发现：
**那个 `hops == 0` 的 guard 今天成立，靠的是无符号下溢这种巧合。**
哪天有人把计数器改成有符号、或者重构这个循环，`hops=0` 就会开始读 XFF，
而**没有任何测试会红**（因为那时它才第一次成为真缺陷）。
已改成 `while (end > 0 && remaining > 0)`，两道 guard 都显式。
现在等价性是设计出来的，不是撞上的。

**这条我想让你复核**：我可能把「巧合正确」判重了。如果你认为这属于过度防御，说。

### 三、我替你做了一个你没明确点头的决定

`Dockerfile` 的 `CMD` 我加了 `--trusted-proxy-hops 0`。人拍板的是**二进制默认值 1**，
我没动那个；但这个镜像自己发布 8088、`docker-compose.yml` 直接映射到宿主，
**前面没有任何代理，链上根本不会出现 XFF**——默认 1 在那里只剩「允许伪造」一个效果。
我判断这是在正确应用人定的规则（参数存在就是为了让部署各自选），不是推翻它。
**如果你或人认为这越界了，回滚这一处即可，不影响其它。**

### 四、T-004 我唯一想说的一点

排除 `prototype/` 之后，我让闸门**打印一行说明它被排除了**。
理由和我上一轮改「无声跳过」是同一条：
**一个「不跑」的决定和一个「忘了跑」的事故，在输出里长得一模一样。**
前者是决策，后者是缺陷；不写出来，半年后没人分得清自己看到的是哪一个。

## 2026-08-07 · T-002 任务书（交给 Codex）+ 我想被质疑的地方

人拍板：T-002 由 Codex 做，作为第一轮完整交叉审查。下面是我能替你省掉的侦察工作，
以及**我希望你不要顺着我的框架走**的地方。

### 靶子

`99d97d3` 在 `MappedShard::open`（`src/query.cpp:93` 附近）删掉了这一行：

```cpp
if (!valid_crawl_date(ent.crawl_date)) return fail();
```

理由写在注释里：历史快照含 `20030229` 这类历法非法值，在这里拒绝会让结构上完全安全的
索引无法服务。**这个理由我认可，不要求你推翻它。** 要补的是它留下的空位。

### 我侦察到的三个事实（省你时间，但请自己复核）

1. **`valid_crawl_date` 本身没变，变的是谁调用它。** `test_core.cpp:91-99` 仍在断言
   `!valid_crawl_date(20230229)` 等 9 条，全绿——**所以现有测试完全感知不到这次放宽**。
   这正是「测试覆盖率看起来没变、实际防线少了一道」的典型形态。
2. **仓库里没有任何「构造一个合法 v2 shard」的夹具。** 现有 `test_corrupt_shard`
   （`test_core.cpp:288`）只写了个 `ShardFileHeader`，把 `entry_count`/`host_count`/
   `url_pool_size` 全填 `UINT32_MAX`，验 `open()` 失败；`test_empty_shard_compatibility`
   写的是 `entry_count == 0` 的空 shard。**两个都不需要真正的 entry/HostBlock/url_pool。**
   所以 T-002 的实际工作量大头在这里：先写一个能按字节拼出合法 v2 shard 的 helper
   （header + HostBlock[] + UrlIndexEntry[] + url_pool，布局见 `CLAUDE.md`），
   有了它，正向和反向用例才都写得出来。**这个 helper 本身比 T-002 值钱**——
   T-006（损坏输入红队）会直接用它。
3. **`verify` 的兜底属实。** 注释声称这些值由 integrity verification 单独报，
   我核过 `src/verify.cpp:423` 确实报。但**这条兜底自己也没有测试**。

### 我认为该有的用例（这是我的框架，不是要求）

- **正向**：含 `20030229` 的合法 shard → `open()` 成功、能查到、`crawl_date` 原样返回。
  没有这条，将来有人把那行加回去，闸门不会红——放宽就会被静默撤销。
- **反向（放宽只放宽了日期这一维，别的必须照旧红）**：magic 不对、header 与文件大小对不上、
  `url_offset + url_len` 越出 url_pool、HostBlock 未按域名排序、entry 未按
  `url_hash` / `crawl_date DESC` 排序、`first_entry + entry_count` 越界。
- **v1 回退路径**：`query.cpp` 387/615/750 三处 v1 fallback 的日期口径是否与 v2 一致？
  如果 v1 路径还在拒绝而 v2 已放行，那是一个未被记录的行为分叉。**我没查，你查。**
- **`verify` 兜底**：给它一个含非法日期的归档，断言它确实报出来。兜底没测试 = 没兜底。

### 硬要求

**每条新断言都要做变异自检**：把对应的检查删掉/改成恒真，确认它确实变红。
写完请在 NOTES-codex.md 里逐条列出你变异了什么、红了没有。
我上一轮踩过的坑就是这类——一个断言可以在被测代码完全坏掉时依然成立。

### 我想被质疑的地方

- **我可能把靶子选小了。** 我盯着日期这一维，是因为它有明确出处。但你读完
  `MappedShard::open` 全段后，如果认为还有别的校验同样无人看守、且更值得先补，
  **推翻我的优先级**，在 NOTES-codex.md 里说明理由，我认。
- **`collab/README.md` 的 10 条红线是我从代码倒推的，不是既有约定**（详见下一节）。
  你实现 T-002 时会大量接触红线 3「索引是不可信输入」——如果你觉得我把实现细节
  误升成了不可动的约束，直接改那份文件。
- **T-001 那个「跳过留痕但不变红」的取舍**，我自己没把握，见下面 T-001 那节第二点。

## 2026-08-07 · T-001：CI 本身很短，值得说的是它周围两件事

`.github/workflows/gate.yml` 只有一条实质命令。真正花时间的是下面两件。

### 一、「闸门在全新 clone 上成立」——我去试了，成立

我在 PLAN 里写 T-001 时那句是「应当成立，但没人验证过」。写完就去 clone 了一份到空目录跑，
退出码 0，四套全绿。**这句话现在可以从「应当」改成「是」。**

之所以专门做这一步：闸门依赖 `sample_data/dat0`（`make test` 里 `./test_parse ../sample_data/dat0 10`
直接读它）。这类依赖在开发机上永远看不出问题——开发机什么都有。
源脚手架那个仓库就栽在同一件事上：闸门在全新克隆上 6/9 失败，因为判题夹具依赖被
`.gitignore` 掉的抓取数据，而这件事是**在闸门运行了一段时间之后**才被发现的。
CI 的附带价值正在这里：runner 每次都是全新 checkout，这条以后不再靠自觉。

### 二、我改了一处「无声跳过」，而且我认为这比 CI 本身重要

原型回归那步的条件是「`sample_data/dat0` 在才跑」。文件不在，这一步就**从输出里消失**——
不是失败，是不存在。读闸门输出的人只会看到剩下几步全绿。

**一个会无声消失的检查，比没有这个检查更糟**：没有的时候你知道自己没有，
消失的时候你以为自己有。这跟我在 NOTES 第三节记的第 3 条（未跟踪文件在 review 包里
只有文件名）是同一类错误的两个实例——**证据在传递过程中被悄悄丢掉，而调用方拿到的
是一个看起来正常的结果。**

现在跳过会打印一行 ⚠️ 留在输出里。但我**没有让它变红**：`dat0` 缺失说明仓库状态不对，
不说明代码坏了，把两者混成同一个信号会稀释红色的含义。这个取舍我不是很有把握——
也可以主张「入库文件不见了就该红」。留给你判。

### 三、我没做的：分支保护

把 `gate` 设成 `main` 的必需检查是权限动作，我没碰。这里只提醒一个已知形态：
必需检查的名字要和 workflow 的 job 名对上，否则会出现「配了必需检查、但配的是另一个
job」这种看起来有防线、实际没有的状态。真要配，配完请用一个临时 PR 验一次再删。

## 2026-08-07 · 脚手架落地：红线是我推的，不是我知道的

搭好了 `collab/` 与 `tools/handoff.py`。本轮不碰 `src/`，所以没有正确性风险；
但有一类**判断风险**值得你先质疑，因为它会影响后面每一轮的审查口径。

### 一、红线清单的来源，以及它可能错在哪

`collab/README.md` 那 10 条红线，我是从 `CHANGELOG.md`（尤其 `29d7469` 那次加固）、
`README.md` 和源码里**倒推**出来的，不是谁交给我的既有约定。倒推有个固有的失真：
**我看到的是「当前实现是这样」，我写下的是「这一条不许动」**——这两者之间隔着一次
我没有依据的升格。

最可能被我升错的两处：

- **红线 2（checkpoint 的 tainted→completed 语义、被中断的源不自动重放）**：
  CHANGELOG 把它描述成一个有意的设计（重放=重复追加=静默污染），我据此写成红线。
  但「不自动重放」的代价是「中断后只能重建整个归档」，对 1400 万篇的全量导入来说
  这个代价相当大。如果 `load.cpp` 里其实留了更细的恢复路径而我没读到，这条应当改写。
- **红线 5（DoS 面的具体阈值：队列 256、30s 超时、30 次/5 秒）**：我把**具体数值**
  写进了红线。数值是可调的工程参数，不是安全边界；真正的红线应该是「这些机制存在」
  而不是「这些数字不变」。我倾向于保留现在的措辞（改数值时至少要被人看见一次），
  但如果你觉得这会在正常调参时制造无谓摩擦，改掉它。

### 二、我对 `99d97d3` 的看法（PLAN T-002）

最近一次提交把 `MappedShard::open` 里对 `valid_crawl_date` 的拒绝删掉了，
理由写在注释里：历史快照含 `20030229` 这类历法非法值，在这里拒绝会让结构上完全安全的
索引无法服务。**这个理由我认可**，而且注释声称「integrity verification reports these
values separately」——我去核了，属实，`src/verify.cpp:423` 确实单独报。

我不放心的是另一件事：**这是一处放宽，而放宽之后没有任何测试站在那个位置。**
现在既没有「含 20030229 的 shard 能被正常服务」的正向用例，也没有「日期字段之外的
结构性损坏仍然被拒绝」的反向用例。也就是说，将来任何人（包括我）在这一段继续往下删，
闸门不会红。放宽本身合理，无人看守不合理。

写 T-002 时我特意没有把它写成「这个提交有问题」——它没问题。问题是它留下的空位。

### 三、闸门工具本身的三个坑，都是跑出来的不是想出来的

我写完 `tools/handoff.py` 后真跑了一遍，三处当场炸/漏。记下来是因为**它们都是
「移植一份看起来能用的脚本」时不会想到、只有真跑才会暴露**的：

1. **闸门自己崩在 `UnicodeDecodeError` 上。** `test_parse` 打印归档正文预览时按**字节**
   截断，收尾常是半个 UTF-8 字符（你在 `make test` 输出里能看到那些 `�`）。
   `subprocess.run(text=True)` 默认严格解码，直接抛异常——**被测代码完全没问题，
   是闸门自己红的**。这在一个专门处理 GB18030↔UTF-8 的项目里格外讽刺。已加 `errors="replace"`。
2. **尾部不是「最后发生的事」。** 原来分别抓 stdout / stderr 再拼接，等于按**流**排序而不是
   按时间排序：`make test` 的 `Leaving directory` 会排在 test_core 的错误行前面。
   而尾部正是交接记录里唯一被引用的部分——排错了就等于引错了。改成 `stderr=STDOUT` 合流。
3. **未跟踪的新文件在 review 包里只有文件名、没有内容。** `git diff HEAD` 不含未跟踪文件，
   所以本轮这种「几乎全是新文件」的交接，审查方打开包会看到一份空改动。
   已改成对每个未跟踪文件跑 `git diff --no-index /dev/null <file>` 生成正规 hunk。
   这里还踩了第二层：`--no-index` **有差异时退出码就是 1**，而原来的 `git(soft=True)`
   在非零退出码时返回空串——**把要的东西连同"错误"一起丢了**。单独加了 `git_out()` 忽略退出码。
   附带的防线：单个未跟踪文件超过 100KB 或不是 UTF-8 文本就跳过，
   免得哪天有人在工作区里留了个归档产物，整个 review 包被灌爆。

第 3 条对本项目尤其要紧：`.gitignore` 里躺着 `data/` `index/` `archive/`，
未跟踪文件里出现 GB 级二进制是**正常状态**，不是意外。

我还做了一次变异自检：把一个语法错误文件塞进 `py_compile` 那步，确认闸门确实变红
（`ok=False`、输出里有 `❌ exit`）。一个从来没红过的闸门不能证明自己会红。

### 四、我对源脚手架做的唯一一处改动，以及为什么

源版本 `run_verify` 在步骤成功时只往 review 包里写 `✅ ok`。但同一个仓库的 README
写着「交回时必须附一次真正跑完的验证结果（`--verify` 输出必须包含全套测试尾部计数）」。
**规则写下来了，工具却把证据丢了**——成功路径上一个 `✅` 和一个「我觉得没问题」
在信息量上是等价的。本版成功时保留每步尾部 12 行（`TAIL_LINES`），
所以 `PASS: 89 core checks` 这类数字会真的进到交接记录里。

如果你觉得 12 行太吵，调 `TAIL_LINES`；但请不要退回到只打印 `✅`。

### 五、还没做、我也不打算替人做的两件事

`PLAN.md` 里 T-004（Python 原型去留）和 T-005（反代后的限流口径）都标了**待人拍板**。
T-005 尤其不该由 agent 决定：信任 `X-Forwarded-For` 意味着任何人伪造一个头就能绕过限流，
这是安全边界的移动，不是实现细节。我只把现状核清楚了写进 PLAN——
限流键是 `getpeername` 的 `sin_addr`（`src/server.cpp:1923–1926`），全仓库没有任何
`X-Forwarded-For` 处理，所以 `docker-compose.yml` 那种部署下「每 IP 30 次/5 秒」
事实上是「全站 30 次/5 秒」。这个事实本身值得人看一眼，不管最后怎么定。
